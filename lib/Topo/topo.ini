;skrypt modu³u tbdBase v0.0.1
;
; klasa obiektu [def <rodzaj> <nazwa-klasy> <nadrzêdny[<ile>]> <podrzêdny[<ile>]> <referencyjny> <warstwa>]
; rodzaj        [none | point | polygon | cell[<nazwa komórki>] | line]
; nazwa-klasy   [unikalny ci¹g znaków klasy obiektów]
; nadrzêdny     [nazwa klasy nadrzêdnej] <ile> - w³aœciwa liczba obiektów nadrzêdnych, (-1 - wiele, 0 - brak)
; podrzêdny     [nazwa klasy podrzêdnej] <ile> - w³aœciwa liczba obiektów podrzêdnych, (-1 - wiele, 0 - brak)
; referencyjny  [-1-pomiñ | 0-nie | 1-tak]
; warstwa       [<1-pomiñ | >0]
; miêdzy obiektami powinny byæ zdefionowane relacje nadrzêdny/podrzêdny (zawiera/jest zawarty|nachodzi|przecina|styka)
;
; definicje klas obiektow
def line linie brak[0] brak[0] 0 1
def cell[1_OSM] osnowa-osm linie[1] brak[0] 0 4
def cell[1_OS2] osnowa-os2 linie[1] brak[0] 0 2
;
; zmiana atrybutów klasy obiektu [kod-atrybutu <nazwa-klasy> <wartosc>]
;ref nazwa-klasy [1|0]
;lev nazwa-klasy warstwa
;
; definicje atrybutów niestandardowych [atr <nazwa-atrybutu> <nazwa-klasy> <wartosc-atrybutu>]
atr klasa osnowa-osm 0
atr klasa osnowa-os2 1
;
; procedury ³adowania obiektów [load <nazwa-klasy>]
load linie
load osnowa-osm
load osnowa-os2
;
; procedury ³¹czenia obiektów podrzêdny/nadrzêdny [rel <nazwa-pod> <nazwa-nad> <rodzaj-relacji>]
; rodzaj-relacji: [zawieranie | przecinanie | styczne | wspolne]
; zawieranie    : tj. numer wewnatrz dzialki (tylko polygon)
; przecinanie   : tj. miedzy obiektami tej samej klasy np. dzialki (tylko liniowe)
; styczne       : tj. punkt na prostej, krzywej (wszystkie)
; wspolne       : tj. co najmniej jeden punkt wspolny (wszystkie)
;
rel osnowa-osm linie wspolne
rel osnowa-os2 linie wspolne
;